# .gitlab-ci.yml

- Definiert die Stages `build` und `test`
- Nutzt `sbtscala/scala-sbt:latest` (JDK + sbt vorinstalliert)
- Cacht Dependency-Verzeichnisse für schnellere Pipelines
- Integriert die Job-Definitionen aus `.gitlab/jobs.gitlab-ci.yml`
---